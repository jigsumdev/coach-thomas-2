---
import Icon from './Icon.astro';

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<li class={`flex items-center gap-3 text-sm text-white/75 ${className}`}>
  <Icon name="check" size={16} />
  <slot />
</li>
